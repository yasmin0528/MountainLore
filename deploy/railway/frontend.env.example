# Replace "backend" if you choose a different Railway backend service name.
# This is a Railway reference variable; paste it in the frontend service's
# Variables page rather than into a checked-in .env file.
BACKEND_API_URL=http://${{backend.RAILWAY_PRIVATE_DOMAIN}}:${{backend.PORT}}/api
